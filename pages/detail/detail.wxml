<!--pages/detail/detail.wxml-->
<view class="head">
    <image src="{{head_image}}"></image>
    <view id="name">{{name}}</view>
    <view id="time">{{time}}</view>
    <view id="price1">price:</view>
    <view id="price2">￥{{price}}</view>
  </view>
<view class="detail_frame">
  <view id="from">From: {{from}}</view>
  <view id="to">To: {{to}}</view>
  <view id="type">{{type}}</view>
  <view id="gender">{{gender}}</view>
  <view id="ddl">Before  {{dates}}  {{times}}</view>
  <view id="description1">Description: </view>
  <view id="description2">{{description}}</view>
    <image class="productImg" src="{{image}}"></image>
 </view> 

  <button class="b1" bindtap="take">Take the order</button>
  <button class="b2" bindtap="chat">Chat</button>

<view class="bottom_blank"></view>